<header class="bg-gray-800 text-white shadow px-6 py-4 flex items-center justify-between">
  <h1 class="text-xl font-bold">CYBORGBot Dashboard</h1>
  <% if (user) { %>
    <div class="flex items-center space-x-3">
      <span class="text-sm text-gray-300">Logged in as <%= user.username %></span>
      <a href="/logout" class="text-red-400 hover:text-red-300 text-sm">Logout</a>
    </div>
  <% } %>
</header>
