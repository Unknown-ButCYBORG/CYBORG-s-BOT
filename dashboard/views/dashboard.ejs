<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title>CYBORGBot | Your Servers</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <script src="https://cdn.tailwindcss.com"></script>
</head>

<body class="bg-gray-900 text-white font-sans min-h-screen">

  <%- include('partials/header') %>

  <div class="max-w-6xl mx-auto px-4 py-8">
    <h1 class="text-3xl font-bold mb-6">Select a server to manage</h1>

    <% if (guilds.length === 0) { %>
    <p class="text-gray-400">You don’t share any servers with the bot.</p>
    <% } else { %>
    <div class="grid grid-cols-1 sm:grid-cols-2 md:grid-cols-3 gap-6">
      <% guilds.forEach(guild => { %>
      <div class="bg-gray-800 rounded-lg shadow hover:shadow-xl transition p-5">
        <h2 class="text-xl font-semibold mb-2"><%= guild.name %></h2>
        <a href="/dashboard/<%= guild.id %>" class="inline-block mt-2 bg-blue-600 hover:bg-blue-700 px-4 py-2 rounded text-white text-sm font-semibold">
          Manage
        </a>
      </div>
      <% }) %>
    </div>
    <% } %>
  </div>

  <%- include('partials/footer') %>

</body>

</html>